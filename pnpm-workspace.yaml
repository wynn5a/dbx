packages:
  - "packages/*"

# Native deps whose install/build scripts must run.
# Lives here (not package.json's `pnpm` field), which pnpm v11+ no longer reads.
onlyBuiltDependencies:
  - better-sqlite3
  - esbuild
  - keytar
  - vue-demi
